{
  "name": "md-to-wordpress-post",
  "displayName": "Markdown to WordPress Post",
  "description": "Publish Markdown files with local media assets to WordPress from Cursor.",
  "version": "1.8.0",
  "publisher": "show-ya",
  "repository": {
    "type": "git",
    "url": "https://github.com/show-ya/VSIX-MD-to-WordPressPost.git"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:mdToWp.publishCurrentFile"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "mdToWp.publishCurrentFile",
        "title": "Publish Current Markdown to WordPress"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "mdToWp.publishCurrentFile",
          "when": "editorLangId == markdown",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Markdown to WordPress Post",
      "properties": {
        "mdToWp.siteUrl": {
          "type": "string",
          "default": "",
          "description": "WordPress site URL. Example: https://example.com"
        },
        "mdToWp.username": {
          "type": "string",
          "default": "",
          "description": "WordPress username for Application Password authentication"
        },
        "mdToWp.applicationPassword": {
          "type": "string",
          "default": "",
          "description": "WordPress Application Password"
        },
        "mdToWp.defaultStatus": {
          "type": "string",
          "enum": [
            "draft",
            "publish",
            "private",
            "pending"
          ],
          "default": "draft",
          "description": "Default status when front matter does not define status"
        },
        "mdToWp.postApiPath": {
          "type": "string",
          "default": "/wp-json/wp/v2/posts",
          "description": "Post API path or full URL. Example: /wp-json/wp/v2/posts"
        },
        "mdToWp.pageApiPath": {
          "type": "string",
          "default": "/wp-json/wp/v2/pages",
          "description": "Page API path or full URL. Example: /wp-json/wp/v2/pages"
        },
        "mdToWp.webhookUrl": {
          "type": "string",
          "default": "",
          "description": "Webhook URL to notify after a successful post publish"
        },
        "mdToWp.webhookSecret": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "description": "Webhook secret sent via X-Webhook-Secret header. Set this in user settings, not a shared workspace file"
        },
        "mdToWp.xPostPrefix": {
          "type": "string",
          "default": "",
          "description": "Optional first line for the generated X post"
        },
        "mdToWp.xPostPostfix": {
          "type": "string",
          "default": "",
          "description": "Optional last line for the generated X post"
        },
        "mdToWp.useXPro": {
          "type": "boolean",
          "default": false,
          "description": "Use the X Pro text limit (25000 characters) instead of the standard 280"
        },
        "mdToWp.postOnRepublish": {
          "type": "boolean",
          "default": true,
          "description": "When true, send the webhook again if a published post is republished with a different publish date"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.2.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^13.0.2"
  }
}
